<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sona.opticalillusions">

    <!--
    android:versionCode="1"
    android:versionName="1.0" ^
    -->
    <!--
<uses-sdk
        android:minSdkVersion="16"
        android:targetSdkVersion="25" />
    -->
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat">
        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="none"
            android:theme="@style/Theme.AppCompat.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
        <service android:name="test"> </service>
        <receiver android:name="test"></receiver>
        <provider android:name="test" android:authorities="test"></provider>
        -->

        <activity
            android:name=".IllusionsGridActivity"
            android:label="@string/title_activity_illusions_grid" />
        <activity
            android:name=".InfoActivity"
            android:label="@string/title_activity_info" />
        <activity android:name=".ImageAdapter" />
        <activity android:name=".IllusionGridAcitivity" />
        <activity android:name=".SquareImageView"></activity>
    </application>

</manifest>